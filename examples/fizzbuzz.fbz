/*
main - function name
(i) - iterator variable. If(i, f, s, ...), then 'f, s, ...' would be parameters
[0, 100] - range for iterator
N >> ... - print ... when i % N == 0
*/

main(i) [0, 20]:
	3 >> "FIZZ"
	5 >> "BUZZ"
	3 + 4 >> concat("SQUARED: ", i * i)
	else >> i
end

concat(i, f, s) [1, 1]:
	1 >> f
	1 >> s
end
